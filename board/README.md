# board —— 上板工程与实测输出

PYNQ-Z2 上板运行工程、启动脚本与实测输出记录。

## 规划内容

- `setup.md`：从零复现指南（SD 卡烧录 → 部署 → 运行演示）
- `run.sh` / 启动脚本：演示与测试的一键入口
- `logs/`：每次上板实测的原始输出日志（按日期归档）
- `media/`：演示照片与视频索引

## 约定

- 每次实测记录：日期、硬件连接、bitstream 版本（commit hash）、结果
- 实测数据是评分硬依据，日志不删不改，只追加

> 状态：🚧 待开发
