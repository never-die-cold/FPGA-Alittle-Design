# 模块一开发计划：自研三级流水 RISC-V 软核

> 指令集 RV32IM ｜ 基线两级流水 → 三级流水（IF / ID+EX / MEM+WB）＋数据转发 ＋ 轻量分支预测
> 三部分按依赖顺序推进，每部分独立可验证、独立出数据；对应 README 中 M1 里程碑（9 月下旬 – 10 月上旬）。

---

## 三部分分工总览

| 部分 | 名称 | 核心内容 | 关键交付物 | 验收标准 | 建议主责 |
|:---|:---|:---|:---|:---|:---|
| A | 两级流水基线核 v0 | 数据通路 + 控制 + RV32IM 全指令，打通工具链闭环 | 可综合 v0 核 + 冒烟 tb + 工具链脚本 | 单指令冒烟全过，上板跑通小程序，**基线 CPI / Fmax 数据在案** | 逻辑开发主力 |
| B | 三级流水重构 + 数据转发 | 拆三级 + 旁路电路 + Stall 控制 | v1 核（无预测版）+ 转发专项 tb | 相关指令序列 CPI≈1，load-use 正确停顿，arch-test 与 v0 同集合全过 | 逻辑开发主力 |
| C | 分支预测 + 验证闭环 | 1-bit/2-bit BHT + 冲刷 + benchmark + 全对比数据 | 完整 v1 核 + benchmark 源码 + 三级对比报告 | CPI 降低 ≥25%，Fmax ≥100 MHz，命中率可统计 | 调试验证（全员支援） |

> 分工不分家：每周例会互讲进度，A/B/C 每个部分的设计决策、波形与数据三人都会看。

---

## 部分 A：两级流水基线核 v0（第 1 周）

**目标**：先把"从 C 编译到板子上跑起来"的闭环打通，拿到后续一切对比的**锚点数据**。

### 范围

- 两级流水：第一级取指（IF），第二级译码+执行+访存+写回
- 数据通路：`pc` / `if_stage` / `decode` / `alu` / `muldiv`（M 扩展，可先斩后补）/ `regfile` / 访存接口
- 控制信号完整，`core_top` 顶层打通
- 配套最小 SoC 外壳：指令 BRAM（预载 hex）+ 一个最小外设（LED / UART 二选一）
- 工具链闭环：riscv-gnu-toolchain 编译 → objcopy 转 hex → 预载入 BRAM → 仿真/上板

### 交付物

- [ ] `v0` 核 RTL（每模块头部注释：功能/接口/作者/日期）
- [ ] 冒烟 testbench：每条 RV32I 指令一个汇编单测
- [ ] 工具链脚本（编译、转 hex、加载流程写死在脚本里）
- [ ] **基线数据**：Vivado 综合报告 Fmax/WNS + 初版 benchmark 实测 CPI

### 验收标准

- 仿真：RV32I 算术/逻辑/访存/分支/跳转指令单测全部通过
- 上板：跑通 LED 闪烁或乘法小程序
- 数据：基线 CPI 与 Fmax 记录进 `report/`，这是之后"降低 ≥25%"的对照锚点

### 依赖与风险

- 依赖：prep_checklist 全部 🔴 项完成（Vivado / 工具链 / 板子）
- 风险预案：M 扩展乘除若卡住，先以 RV32I 出基线数据，乘除放 Part A 收尾补

---

## 部分 B：三级流水重构 + 数据转发（第 2 周）

**目标**：完成本项目最核心的创新点——拆级提频 + 旁路消气泡。

### 范围

- 插入流水寄存器，重构为三级：`if_stage` / `id_ex_stage` / `mem_wb_stage`
- 数据转发（旁路）单元 `forwarding.v`：EX→EX、MEM→EX、WB→EX 三条旁路
- 冒险检测 `hazard.v`：load-use 停顿（stall）、控制冒险处理（先冲刷后预测，Part C 换 BHT）
- 控制信号随流水级逐级传递与裁剪
- 功能一致性：与 v0 跑**同一套**测试集合，保证"优化不改语义"

### 交付物

- [ ] v1 核（无分支预测版）
- [ ] 转发专项 tb：back-to-back RAW 依赖序列（R-type 连续相关、lw→add 等），波形可数气泡
- [ ] 中期数据：同 benchmark 在 v0 与 v1（无预测）上的 CPI 对比

### 验收标准

- arch-test 与 v0 相同集合全过
- 连续相关 R-type 序列 CPI 接近 1（旁路生效，波形零气泡）
- load-use 场景正确插入 1 拍停顿
- Vivado 综合：v1 相比 v0 主频有提升或持平，WNS ≥ 0

### 依赖与风险

- 依赖：Part A 的 v0 测试集合与数据
- 风险预案：若拆级后时序仍不达标，回退为"两级 + 完整转发"（L3 保底版），优化深度不打折

---

## 部分 C：分支预测 + 验证闭环（第 3–4 周）

**目标**：BHT 落地 + benchmark 完整化 + 出一份有说服力的三级对比数据。

### 范围

- `branch_predict.v`：1-bit / 2-bit 饱和计数器 BHT，做成**可配置切换**（1-bit/2-bit/关闭三档，便于对比实验）
- 预测错误冲刷（flush）逻辑：PC 恢复、流水寄存器清零
- 命中率统计计数器（片上统计或仿真统计，数据要有来源）
- 自写 benchmark：Dhrystone 思路的整型测试（循环/数组/函数调用/位运算/乘法混编），C 源码 + 反汇编归档
- riscv-arch-test 扩展子集（RV32M 等）跑全
- 最终数据与报告：v0 / v1 无预测 / v1+预测 三档的 CPI、Fmax、资源（LUT/FF/BRAM）、命中率

### 交付物

- [ ] 完整 v1 核（三级 + 转发 + 可切换 BHT）
- [ ] benchmark 源码与编译脚本（`sw/riscv_fw/`）
- [ ] 对比数据报告（三档数据表格 + 测试条件 + 原始日志），归档 `report/`
- [ ] 验证脚本/清单沉淀，标 `#skill候选`（如"CPI 测量流程"）

### 验收标准

- CPI 相对 v0 基线降低 ≥ 25%（README 指标）
- Fmax ≥ 100 MHz，WNS ≥ 0
- 2-bit BHT 在含循环的 benchmark 上命中率可统计且显著优于无预测
- 全套测试回归通过，数据与 git commit 对得上

### 依赖与风险

- 依赖：Part B 的 v1 主体
- 风险预案：分支预测若排期吃紧，保底交付"关闭 BHT + 默认冲刷"版本（L3 保底版），三档数据退化为两档

---

## 技术栈全景

| 层 | 技术 | 用途 | 对应部分 |
|:---|:---|:---|:---|
| 硬件描述语言 | Verilog-2001（综合）+ 简单 SystemVerilog（tb 用） | 全部 RTL 与验证 | A/B/C |
| 指令集 | RV32IM 规范（riscv.org 手册） | 指令编码、语义唯一权威来源 | A |
| 微架构理论 | 流水线/冒险/转发/预测/CPI（COAD RISC-V 版、胡伟武开源书） | 设计依据与答辩理论 | B/C |
| 软件工具链 | riscv-gnu-toolchain（RV32IM, ILP32）+ objdump/objcopy + linker script | C/汇编 → elf → hex 预载 | A/C |
| 仿真 | Vivado XSim（备选 iverilog/Verilator） | tb 仿真、波形分析 | A/B/C |
| 综合实现 | Vivado 综合/实现 + 时序约束 + WNS 分析（UG901/UG904/UG949） | Fmax/资源报告 | A/B/C |
| 验证基准 | riscv-arch-test + 自写 tb + benchmark（Dhrystone 思路） | 功能正确性与 CPI 量化 | A/C |
| 参考设计 | 蜂鸟 E203、PicoRV32 | 流水线/转发/预测实现对照 | B/C |

---

## 学习路线（分阶段，与三部分绑定）

> 每条资料的具体链接与用途已单列在 [docs/resources.md 第 9 节](../../docs/resources.md)，本节只保留条目 + 自测验收。

### 阶段 0：公共基础（开工前 1 周，全员）

| 学习内容 | 资源（docs/resources.md） | 自测验收 |
|:---|:---|:---|
| Verilog 基本功：语言 + 时序电路 | §9.1：HDLBits Verilog Language + Sequential Logic 题集 | 每天 5 题无压力，能独立写状态机 |
| 体系结构理论：单周期数据通路 | §9.1：COAD 第 4 章 4.1–4.5 | 能徒手画出单周期 RV32I 数据通路并标注控制信号 |
| RISC-V ISA：RV32I 指令编码 | §9.1：RISC-V 官方手册第 2 章 + 编码速查卡 | 给定汇编指令能手算机器码；给定机器码能反汇编 |

### 阶段 1：对应 Part A（第 1 周）

| 学习内容 | 资源（docs/resources.md） | 自测验收 |
|:---|:---|:---|
| 中文 CPU 设计实践对照 | §9.2：蜂鸟 E203 书前 3 章 + PicoRV32 源码速读 | 能说出 E203 与我们的两级结构差异 |
| RISC-V 工具链实操 | §9.2：riscv-gnu-toolchain 安装与使用 | 独立完成：C 编译 → objdump 反汇编 → objcopy 转 hex |
| （推荐）单周期迷你核练手 | §9.2：Nandland 教程辅助 | 一天内跑通加法/跳转两条指令 |

### 阶段 2：对应 Part B（第 2 周）

| 学习内容 | 资源（docs/resources.md） | 自测验收 |
|:---|:---|:---|
| 流水线与冒险理论 | §9.2：COAD 4.6–4.9 + 胡伟武书流水线章 | 能区分 RAW/WAR/WAW，说出 load-use 为何必须 stall |
| 转发/停顿工程实现 | §9.2：蜂鸟 E203 流水线章节精读（long-pipe 结构） | 能画出三级流水时序图并标出三条旁路路径 |
| 波形阅读 | §9.2：UG900（XSim 波形操作） | 能从波形数出气泡个数，判断转发是否生效 |

### 阶段 3：对应 Part C（第 3–4 周）

| 学习内容 | 资源（docs/resources.md） | 自测验收 |
|:---|:---|:---|
| 分支预测 | §9.2：COAD 预测章节 + E203 对应实现 | 能解释 2-bit 饱和计数器为何抗"单次抖动" |
| 验证方法学 | §9.3：riscv-arch-test README（编译/比对 signature 机制） | 能独立新增一条 arch-test 用例并跑通 |
| benchmark 与 CPI 测量 | §9.3：Dhrystone / CoreMark / Embench | 能说清"指令数 + 周期数 → CPI"的统计口径 |
| 时序分析与报告阅读 | §9.3：UG949 速览 + UG901/UG904 读报告 | 能读综合报告，定位最差路径（WNS 怎么看） |

---

## 分工建议（映射到 README 组队分工）

| 角色 | 主攻部分 | 侧重路线 |
|:---|:---|:---|
| 逻辑开发主力 | A 主写 + B 主写 + C 支援 | 阶段 0→1→2 深钻；RTL 编码风格、微架构工程实现 |
| 调试 + 验证 | A 支援 + B 支援 + C 主测 | 阶段 0→1→3 深钻；tb 方法学、arch-test、benchmark、时序分析 |
| 文档与答辩 | 全程记录设计决策 | 阶段 0 全通 + 理论吃透（CPI 推导、冒险分类）；每个决策点记 llm_log |

---

## 里程碑对齐

| 周次 | 交付 | 对应 README 节点 |
|:---|:---|:---|
| 开工前 | 阶段 0 完成（HDLBits / 理论 / ISA） | 报名与选型收尾 |
| 第 1 周 | Part A 完成：v0 基线 + 基线数据 | M1 前半 |
| 第 2 周 | Part B 完成：v1 无预测版 + 转发验证 | M1 后半 |
| 第 3–4 周 | Part C 完成：BHT + benchmark + 三档对比报告 | M1 里程碑验收：仿真全过 + CPI/主频数据出炉 |
